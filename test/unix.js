
'use strict';

let unix = require('../lib/unix');


describe('# test unix file', () => {

    it('find pid from port', () => {

        var res2= [1]
        expect(res2).to.be.a('array');
        res2.length.should.be.equal(1);

    });
});